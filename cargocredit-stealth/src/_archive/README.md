# Archived — Pre-Stealth Placeholder

Original interactive landing page (Hero, ValueProps, scroll-driven supply-chain graph,
overlays, email form). Moved here on 2026-04-17 when the public site was replaced with
a minimal stealth placeholder.

Excluded from TypeScript compilation (`tsconfig.app.json`) and Tailwind content scan
(`tailwind.config.js`) so it neither blocks builds nor ships in the bundle, but stays
one rename away from restoration.

To revive a component, move it back into `src/components/` and re-wire `src/App.tsx`.
